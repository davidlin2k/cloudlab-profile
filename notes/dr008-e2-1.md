# dr008-e2-1: the verified-aligned re-run (DR-008 E2, 2026-09-26 15:04-15:55Z)

Facts only. DR-008 E2, pre-registered prediction: 0 wedges for cells
that pass the manipulation gate. Node @clnode366 (n1), fresh boot
14:37:09Z, steering by ntuple rules (5 W1 flows -> queue 7), threaded=1.

## Wiring per cell (metastab.sh M 10 e2-N pin8)

ch7's NAPI kthread (napi_id 8263) pinned to CPU 8 (its IRQ core) inside
the cell wiring; IRQ 312 -> CPU 8; k2_rx on core 8; flood 790k (5x158k)
-> WEDGE -> hold 10 s -> reduce 158k -> 120 s monitor -> 20 s -> 30 s
10k probe. PROBE-OK = probe advance >= 270,000.

## Manipulation gate (per DR-008: aff_change ~0 throughout, mask==8)

1 Hz sampler (p1/e2_sampler.py): ch7 thread Cpus_allowed_list,
/proc/irq/312/effective_affinity_list, ch_stats.aff_change + events
(kcore). Cell counts only if mask==8 on every sample and aff_delta<=5.

## Result: 8/8 cells PASS the gate AND recover

| cell | verdict | adv | aff_delta | mask mismatches |
|---|---|---|---|---|
| e2-1 | PROBE-OK | 300096 | 0 | 0/203 |
| e2-2 | PROBE-OK | 300097 | 0 | 0/203 |
| e2-3 | PROBE-OK | 300096 | 0 | 0/203 |
| e2-4 | PROBE-OK | 300098 | 0 | 0/203 |
| e2-5 | PROBE-OK | 300098 | 0 | 0/203 |
| e2-6 | PROBE-OK | 300099 | 0 | 0/203 |
| e2-7 | PROBE-OK | 300096 | 0 | 0/203 |
| e2-8 | PROBE-OK | 300098 | 0 | 0/203 |

adv ~300,097 = the probe ceiling (10k kbps, 30 s, fully delivered).
The detour counter never moved: with the poll on its IRQ core the
driver returns full budget and repolls (the en_txrx.c busy path); the
affinity detour (work_done--) never fired.

Prediction confirmed: 0/8 wedges, exactly as pre-registered in
decisions/DR-008.md.

## Two discarded attempts (recorded for the record)

- Attempt 1 (15:04-15:19, cells e2-1..3): the pin was applied before
  the cell, but metastab's own wiring block re-applied the unpin (mask
  0-63 ~2 s into every cell) -- cell-1 aff_delta=50154, cells 2-3
  aff_delta=0 but mask 0-63 (the thread happened to stay on cpu 8).
  All three fail the gate as specified; discarded. Fix: metastab.sh
  gained the WIRE argument so the pin is part of the cell's own wiring.
- Attempt 2 (15:25-15:29): sampler/verdict pointed at M10-e2-N while
  metastab names cells M1-e2-N (NKEEP = word count of "10" = 1). The
  sampler crashed on the missing dir; no cells counted. Fix: the
  correct cell dir + the sampler creates its outdir.

Raw: analysis/dr008-e2/e2-artifacts.tar.gz (e2.log, 8x sampler csv +
cell.env + affinity-pre.txt). Note the affinity-pre.txt napi_aff line
records the wiring applied by metastab (pin8 -> "8").
