# 2026-09-25 — W3 pre-registration (DR-004 task 4, step 1 of the run order)

Status: COMPLETE. The W3 spec is frozen BEFORE any W3 cell, per the run order
in DR-004 (pre-register first: predicted knee, kill criterion, >=5x wake-delay).

## What ran (calibration only — cal-2 void, cal-3 superseded, cal-3b is data)

- Toolchain: memcached 1.6.24 installed; mutilate built (py3 SConstruct port)
  and deployed to tx0-tx4 (md5 96f66318...); three defects found and fixed:
  missing libevent-2.1/libzmq on senders (silent apt failure), Generator.cc
  segfault on bare distributions (strtok_r on NULL; patch in
  p1/patches/Generator.cc.upstream), memcached defaulting to 127.0.0.1.
- Load model pinned from source + law: `--update 0.1` = 90% GETs
  (Connection.cc:127); achieved QPS = -q / c per master (10 masters, +/-0.2%).
- Steering verified: ntuple rule tcp4 dport 11211 -> queue 7; landing purity
  99.93-99.95% over gated bursts.
- Instrument correction: queue 7's threaded-NAPI kthread (PID 1594438) wanders
  (found pinned to 9 before calibration); per-CPU costs REQUIRE pinning it to
  CPU 8 (W1/W2 semantics). cal-3's split is superseded for this reason.

## Pre-registered numbers (specs/p1-W3MEMC.md, FROZEN)

- P0 cost 16,130 ns/req -> knee 62.0k QPS (bar 46.5-77.5k).
- P0X receive 6,066 ns + app 12,138 ns -> knee 82.4k QPS (bar 61.8-103.0k).
- Kill criterion (P2): at 2.0x knee with open-loop verified, goodput >= 90% of
  knee = the collapse does not survive TCP (claim dies); < 70% = it survives.
- Wake-delay (P3): p99(P0)/p99(P0X) >= 5 at the knee.
- SLO basis: idle p99 104-106 us, SLO = 10x = 1.05 ms.

## Next (run order step 2)

Knee-pass: 30k-130k x 1 rep x {P0, P0X} (14 cells), then the main matrix
(5 loads x 3 reps x 3 arms = 45 cells), then perf sched at 0.25x (2 captures).
