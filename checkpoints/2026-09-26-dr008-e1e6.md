# Checkpoint 2026-09-26 (2): DR-008 committed; E1 and E6 landed same hour;
# minimization table delivered with the A4 flag

## DR-008

Committed verbatim as decisions/DR-008.md (provenance header only; commit
079c30d), mirrored. Factual note in the header: at receipt PERSIST (A0-A8)
had not started (spec + Amendment A frozen only); rxfuzz minimization was
in flight and completed 09:36:01Z before execution began.

## E1 -- fresh-boot default-pinning check: PREDICTION CONFIRMED

Fresh boot 14:37:09Z (reboot issued 14:35:14Z). threaded default = 0, zero
napi kthreads present. `echo 1 > /sys/class/net/enp195s0np0/threaded`
created 64 kthreads: **every one Cpus_allowed_list = 0-63** (unpinned;
PI's source prediction was "0-63" -- exact match). Fresh-boot IRQ
placement (before any of our wiring): comp_i -> cpu 8+i (comp0@8, comp1@9,
comp2@10, comp3@11, comp4@12...), **comp7 = IRQ 312 -> cpu 15** -- a single
CPU the kthread has no binding to. Default exposure is now an empirical
fresh-boot fact, not just a source read. Raw log:
analysis/dr008-e1-freshboot.log (committed).

## E6 -- lore novelty search: NONE FOUND

Four query families against lore.kernel.org (web-indexed): exact
`mlx5e_channel_no_affinity_change` (only UMR/RDMA noise); "threaded napi
affinity stall poll wrong cpu" (zero results); "affinity" + "threaded napi"
+ bailout/reschedule/complete_done (zero results); "napi thread" + "wrong
cpu"/"affinity change" (one stale stable-rc review list, irrelevant).
Recorded as none-found; caveat: web-indexed coverage of lore is partial;
the memo's own caveat stands.

## Minimization table (DR-007 deliverable) -- notes/p2-RXFUZZ-2.md

Per-hit minimal wedging sets (dims whose single reset removes the hit):
015 {threaded}; 038 {adaptive_rx, striding_rq}; 056 {all 8}; 069 {six
backlog dims, threaded absent}; 086 {}; 089 {threaded + six others}.
Intersection across the 6: EMPTY. Reduced-config confirms: 086/089 wedge at
all-defaults 3/3; 056/069 clean 0/3.

**A4 same-hour flag (069): FIRES** -- threaded absent from 069's set; the
cell wedged under threaded=1 as well (threading-agnostic). 038 likewise.
089: rule does not fire, but threaded=0 (the kernel default value) was
NECESSARY there. Both directions keep the default kernel in the wedging
set; PERSIST A4/A5/A0 are the controlled read (queued next).

## Next per DR-008 priority order

E2 (verified-aligned re-run, per-second manipulation logs) on the freshly
booted node; then build + smoke + launch PERSIST (A0-A8, one run at a
time); E3 needs a c6420/i40e reservation (portal action -- flagged to the
user); E4 = desk check of CloudLab hardware list for ConnectX-3; E5 fix
variants after E2; E7's trace half queued.

Committed: DR-008.md, p2-RXFUZZ-2.md, this checkpoint, minimize log, E1
log; pushed; mirrored.
