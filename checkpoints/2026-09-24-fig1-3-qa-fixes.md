# 2026-09-24 — fig1–3 QA round-3 fixes applied + fig1 knee contract fix

Verdict: all 3 round-3 defects fixed and verified at zoom (full-image pass
+ zoomed crop per defect site), plus 1 contract defect the QA rounds missed
(fig1 x-axis normalization). Draft re-affirmed. Re-rendered from the
complete 111-row dataset (matches "dataset complete, 111 rows", skeleton
Fig 1 status).

## Fixes applied (analysis/p1_figures.py)

| # | Defect (source) | Fix | Verified |
| --- | --- | --- | --- |
| 1 | fig1 frameless legend collides with x-label "offered rate / knee" (QA r3) | legend moved below the x-label (bbox anchor -0.22 -> -0.32), ncol 3 -> 2 (3 columns overran the 3.5in width) | zoom crop: clear gap ~half a text height; legend entries do not touch |
| 2 | fig1 "offered" reference line clipped at top-right (QA r3) | kept the line (QA r3: clip acceptable for a reference line) + y-limit floor 800k per the findings' alternative | reference exits top edge by design; no DATA clips anywhere in the frame |
| 3 | fig2 first x category labels collide ("inline sep.app" vs "thr app-core") (QA r3) | tick labels rotation 15 -> 45, fontsize 8 -> 7 (fig2 AND fig3 -- same label set, fig3 had the same latent collision) | zoom crops both figs: every label pair separated; not cut at bottom |
| 4 | fig3 red "+0 hidden" annotations overlap white in-bar values 1113/1466/1451 (QA r3) | moved above the bar, next to/under the total label (offset +11pt from bar top instead of -9pt into the bar) | zoom crop: "+0 hidden" clears in-bar values, totals, neighbouring labels, legend |
| 5 | NEW -- fig1 x-axis knee contradicted the paper contract | axis knee pinned to 525k (the paper's knee); the deliv>=0.95 auto-knee (450k with cal-1 rows) is now a printed diagnostic only | x=2.0 is now 1050k = "2x the knee" (abstract), sweep spans 0.25–2.5x = 130k–1300k (skeleton Fig 1) |

## Defect 5 detail (why it mattered)

The script normalized fig1's x axis by the auto-detected >=95%-delivery
knee (450k with cal-1 rows loaded; 390k without). The paper's contract
says otherwise: skeleton Fig 1 "Goodput against offered rate (0.25–2.5 x
knee)" = exactly the 130k–1300k sweep at knee 525k, the dataset-complete
table labels 525k as "1.0x knee", and the abstract fill "at 2x the knee"
is 1050k offered. With the auto-knee the fig would have shown 1050k at
x=2.33 (and 390k-normalized renders clip the 1300k points at the old
xlim 3.05). The >=95% knee (450–525k bracket) is still printed and
cited in text; it just no longer feeds the axis.

## Verification record

- Render command:
  .venv/bin/python analysis/p1_figures.py analysis/rows-fig1-3.csv
  analysis/rows-fig1-3b.csv analysis/rows-fig1-3c.csv
  analysis/rows-smoke46.csv analysis/rows-cal-1.csv analysis/out
- "merged 111 rows (dropped 5 invalid runs)" == dataset-complete count.
- verify_anchors: both VERIFIED (P0 knee bracket 390k good / 790k
  collapsed; low-rate p50 P0X 12us < P0 128us).
- Print trail: knee (P0, W1): 450000 pps at mean deliv>=0.95 (diagnostic);
  fig1 x-axis knee: 525000 pps.
- Vision QA: full-image pass x3 (no text collisions, legend clear of the
  x-label, no curve cut off at the right edge -- the 1300k endpoints at
  x=2.5 are inside the frame now) + zoomed crops x4 (fig1 bottom strip,
  fig2 label strip, fig3 bar tops, fig3 label strip). Round-3 lesson
  applied: zoom per defect site, not just full-image.
- Label geometry: at rotation 45 the perpendicular separation of adjacent
  label baselines is pitch*sin45 ~ 28pt vs ~7pt cap height -- touching is
  geometrically impossible at 5 labels / 3.5in. (Axis-aligned bbox overlap
  tests are misleading for rotated text: they report -4.9pt for the first
  pair while the rendered labels are clearly separated.)

## Decisions in force

- fig2/fig3 stay 3.5in single-column (with fig1). QA r3's ">=9in width"
  alternative would break the figure set's column width; label collision
  solved at 3.5in via rotation/fontsize instead.
- Offered reference line keeps its top-edge exit (reference line, not
  data; explicitly allowed by QA r3).
- fig1 box tightened to (0, 2.6): the 0.25–2.5x sweep plus pad, so no
  data can ever clip at the right edge (skill gotcha).
- Input set for fig1–3 renders: rows-fig1-3 + fig1-3b + fig1-3c +
  smoke46 + cal-1 (last file wins per (policy,workload,rate,rep)).
- Draft contract fills [X]% (0.20% [0.20,0.20] at 2x knee = 1050k;
  0.13% [0.05,0.21] at 2.5x) are unaffected: data rows unchanged.

## Data locations

- Script: analysis/p1_figures.py (all fixes in one commit with this note).
- Figures: analysis/out/fig{1,2,3}.{png,pdf} (re-rendered 2026-09-24).
- Rows: analysis/rows-{fig1-3,fig1-3b,fig1-3c,smoke46,cal-1}.csv.

## Next actions (unchanged from fig1-3-draft checkpoint)

1. Fig 4 adjudication on rows-fig4.csv (chain6); form F verdict within
   25% or rule 4 + decision record.
2. P1 kernel session (t6) for the deferral arm (fig1 P1 line + fig 7).
3. W5 + p1ctl.py (Fig 5) after Fig 4; Fig 10 blocked (PI call).