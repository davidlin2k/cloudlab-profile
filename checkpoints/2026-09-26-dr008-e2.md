# Checkpoint 2026-09-26 (3): E2 verified-aligned re-run -- 8/8 recover,
# gates clean; prediction confirmed; Sept-24 aligned refutation retired

## What ran

DR-008 E2 on the freshly booted n1 (boot 14:37:09Z). 8 cells, each =
metastab.sh M 10 e2-N pin8: ch7's NAPI kthread (napi_id 8263) pinned to
CPU 8 (its IRQ core) by the cell's own wiring, IRQ 312 -> CPU 8, k2_rx
core 8, flood 790k -> WEDGE -> reduce 158k -> 120 s monitor -> 20 s ->
30 s 10k probe. Manipulation sampler 1 Hz from 4 s in (thread mask,
irq312 effective affinity, ch7 aff_change/events via kcore).

## Verdict vs the pre-registered prediction

Prediction (decisions/DR-008.md): 0 wedges for cells passing the gate.
Result: 8/8 PROBE-OK (adv 300,096-300,099 = probe ceiling, fully
delivered), 0 latches; every cell's gate clean (aff_delta=0,
mask_mismatch=0/203). The detour counter never moved -- on-core polls
return full budget and repoll; the affinity detour (work_done--) never
fired. The Sept-24 aligned-arm refutation (manipulation check failed:
aff_change 598-1065/s) is retired; AN-003 carries the addendum.

## Discarded attempts (recorded, superseded by the fixes)

1. Pin applied before the cell; metastab's own wiring re-applied the
   unpin ~2 s in (mask 0-63; cell-1 aff_delta=50,154). Fix: metastab.sh
   gained WIRE (unpin|pin8) so the wiring is the cell's own.
2. Sampler/verdict pointed at M10-e2-N; metastab names cells M1-e2-N
   (NKEEP = word count of KEEP). Sampler crashed on the missing dir.
   Fix: correct dir + sampler creates its outdir.

## Also fixed this session (platform fragility)

- Steering: the RSS-key route never worked (p1prep's hkey always failed
  on format -- confirmed in the ORIGINAL prep.log); the reboot
  re-randomized the key and the W1 sports hashed to rx40. preflight.sh
  now installs the 5 W1 ntuple rules idempotently before asserting.
  (A latent fragility exposed by the reboot; now deterministic.)
- metastab.sh gained THREADED/CORE/RATE/QUIET args (needed by PERSIST
  arms A4/A5/A6/A0).

## Next

PERSIST smoke (one A8 cell) then the full 7-arm x 10-round batch
(~5-6 h). E4 desk answer: ConnectX-3 exists on CloudLab (m400 ARM64,
m510 x86 -- both Utah; E4 feasible pending a reservation). E3 needs the
c6420/i40e reservation (portal -- user action).

Files: notes/dr008-e2-1.md, anomalies/AN-003.md (addendum),
analysis/dr008-e2/e2-artifacts.tar.gz, p1/metastab.sh (WIRE +
THREADED/CORE/RATE/QUIET), p1/e2_sampler.py, p1/e2_batch.sh,
p1/preflight.sh (ntuple rules), p1/persist.sh (next).
