# DR-005: Instructions for September 28 – October 9

Provenance: relayed verbatim by the user in session bg_013552_b7f304 on
2026-09-25 (UTC), written against the student's actual tooling
(`wdiagtrace.sh`'s wiring, hosts, ports and wedge detector). Committed
as-is per the standing rule: this header only, the memo body below is
unedited. The run order and its "What I need from you, in order" list are
executed literally and in sequence. (The same-day PI review that briefly
held this number is preserved at decisions/DR-005-pi-review.md.)

---

# DR-005: Instructions for September 28 – October 9

## Why the direction changed

The pre-registered A/B ruled out the affinity bailout, and the trace (AN-006A) shows a self-sustaining loop:
1. The receive ring has no posted buffers.
2. So no completions arrive.
3. So no interrupt fires.
4. So no poll runs.
5. So no buffers are reposted.

If that loop keeps the queue dead after the load drops to a normal level, it is a **metastable failure inside the kernel's receive path**. To my knowledge that hasn't been shown before. It would also justify a tool that searches for such failures systematically. That's the OSDI-shaped contribution. Our earlier measurement results (collapse, cost visibility, wake latency) become motivation.

**Paused:**
- the IRQ-time-accounting rebuild;
- DNS;
- XDP admission;
- workshop writing.

Workshop writing resumes after task 1's verdict.

**Rules:**
- Everything follows the handbook: spec before runs, anomaly reports within 24 hours, supersede and never delete.
- Every new script gets a short smoke run before any batch.
- Commit every script before it runs.

## Task 1: the metastability test (Monday, September 28)

**Question:** after a wedge begins, does the queue stay dead when the offered load falls to a level it normally handles?

### Step 1: freeze the spec (by 10:00)

Create `specs/p1-METASTABLE.md` before any run. Fixed definitions:

| Item | Value |
| --- | --- |
| Arm | `unpin`, wired exactly as `wdiagtrace.sh` does it: threaded=1, every `napi/enp195s0np0-*` kthread at affinity 0–63, IRQ 312 → CPU 8, `k2_rx --port 7777 --core 8` |
| Flood | 5 senders × 158k packets/s = 790k (hosts 10.10.1.10–14, the existing source ports) |
| Reference capacity | 525k packets/s, the "1.0× knee" of the Fig. 1 contract |
| Reduced levels | **M158**: keep sender .10 only (158k, 30% of reference). **M316**: keep senders .10 and .11 (316k, 60%) |
| Baselines | **B158** and **B316**: the same kept senders for 120 s with no flood beforehand, same wiring |
| Wedge detector | Unchanged from `wdiagtrace.sh`: `rx7_packets` flat, and `rx_out_of_buffer` rising more than 50k per 2 s, for 3 consecutive 2 s samples |
| Recovered | `rx7_packets` advancing at ≥90% of the kept offered rate for 3 consecutive 2 s samples. Recovery time = the start of the first of those samples, computed offline from `counters.log` |
| Reps | 5 per cell type (M158, M316, B158, B316) |

Pre-registered outcome classes, per reduced level:

| Class | Rule |
| --- | --- |
| **Metastable** | At least 4 of 5 M cells not recovered within 120 s, and the matching B cells healthy in 5 of 5 |
| **Not metastable** | At least 4 of 5 M cells recover within 2 s of the reduction |
| **Intermediate** | Anything else. Report the distribution; run 5 more reps; make no claim |
| **Invalid** | Any B cell wedges. The M result at that level can't be interpreted; use the other level |

### Step 2: write `p1/metastab.sh` (by 11:00)

Copy `p1/wdiagtrace.sh` and make exactly these changes:

1. **Arguments.** `MODE` (`M` or `B`), `KEEP` (`"10"` or `"10 11"`), `REP`. Output directory `/root/p1/metastab/$MODE$NKEEP-$REP`, where `NKEEP` is the number of kept senders.
2. **Wiring.** Keep only the `unpin` arm's wiring block.
3. **Tracing.** Remove `trace-cmd` by default. Add `TRACE=1` to enable it, for rep 1 of M316 only.
4. **Durations.** Senders `--secs 400`; `k2_rx --secs 400`.
5. **In mode B:** start only the kept senders, then run the recovery monitor below for 120 s, with no flood and no wedge detection.
6. **In mode M:** after `WEDGE` is detected, hold 10 s. Kill every sender not in `KEEP`, log `REDUCE`, then run the monitor.

The monitor (used in both modes):

```bash
NKEEP=$(echo $KEEP | wc -w)
for o in 10 11 12 13 14; do
  case " $KEEP " in *" $o "*) continue ;; esac
  ssh -n -o StrictHostKeyChecking=no -o ConnectTimeout=8 davidlin@10.10.1.$o 'sudo pkill -xc k5blast; true' 2>/dev/null || true
done
echo "REDUCE mono=$(awk '{print $1}' /proc/uptime) keep=$KEEP" | tee -a "$O/cell.env"
NEED=$((NKEEP * 158000 * 2 * 9 / 10))   # packets per 2 s sample at 90% of the kept rate
PREV_W=$(ethtool -S $IFACE | awk '/rx7_packets:/{print $2}'); OK=0; REC=0
for i in $(seq 1 60); do
  sleep 2
  W=$(ethtool -S $IFACE | awk '/rx7_packets:/{print $2}')
  DW=$((W - PREV_W)); PREV_W=$W
  if [ "$DW" -ge "$NEED" ]; then OK=$((OK + 1)); else OK=0; fi
  if [ "$OK" -ge 3 ]; then REC=1; echo "RECOVERED mono=$(awk '{print $1}' /proc/uptime)" | tee -a "$O/cell.env"; break; fi
done
[ "$REC" = 0 ] && echo "NOT-RECOVERED-120s" | tee -a "$O/cell.env"
```

7. **Afterwards, in both modes:** stop all senders, wait 20 s, then send the 10k packets/s probe from .10 for 30 s. Log `PROBE-OK` if `rx7_packets` advanced by at least 270,000 during the probe (90% of 300,000), and `PROBE-DEAD` otherwise.
8. **Mechanism snapshot, M316 rep 1 only, 30 s after `REDUCE`:**

```bash
devlink -j health diagnose pci/0000:c3:00.0 reporter rx > "$O/rx-diag-reduced.json" 2>&1
ethtool -S $IFACE | grep -E '^ +(rx7_|ch7_)|rx_out_of_buffer' > "$O/ethtool-reduced.txt"
```

First confirm the device handle with `devlink dev show` and the reporter name with `devlink health`. If a command fails, record the exact error in the cell log; don't guess a syntax.

### Step 3: smoke test, then batch (11:00–14:00)

- **Smoke:** one M316 cell and one B316 cell. Check that each creates `cell.env`, a `counters.log` at 1 Hz, and exactly one verdict line.
- **Batch order:** B316, M316, B158, M158, repeated for 5 rounds. Interleaving keeps drift out of any one cell type.

### Step 4: if a queue stays dead after the probe

1. Stop all senders.
2. `echo 0 > /sys/class/net/enp195s0np0/threaded`, wait 5 s, then `echo 1`.
3. If still dead, reset the channels with `ethtool -L enp195s0np0 combined 32` and rerun the **full pre-flight**: RSS key, port map, IRQ re-pin, rediscovering the queue-7 NAPI thread.

Log every reset in the cell's directory. A cell that needed a reset still counts: its verdict is `PROBE-DEAD`.

### Step 5: records (by 16:00)

- `analysis/rows-metastab.csv`, columns: `cell, rep, mode, keep, t_flood, t_wedge, t_reduce, recovered, t_recover_s, rx7_rate_reduced, oob_rate_reduced, probe`.
- `p1/metastab_eval.py`, which prints the pre-registered class for each level.
- A result note, a FINDINGS entry, and an AN-006 addendum.
- Ping me with the class before writing any interpretation.

## Task 2: the mechanism during a stall (Tuesday, September 29)

**Question:** during a wedge, are completions pending with no event (A), or does the ring hold no posted buffers (B)? And what restarts the queue?

1. **State dumps.** Take the `devlink -j health diagnose ... reporter rx` dump three times: during a healthy flood (before the wedge), 10 s into a wedge, and 30 s after `REDUCE` (task 1 already captures that one). Record the queue-7 RQ, CQ and ICOSQ fields, and write down which JSON fields exist before comparing values. Compare the posted-buffer counts and the CQ consumer and producer indices.
2. **Recovery source.** Check `ls /sys/kernel/btf/mlx5_core`.
   - **If it exists,** run for the whole cell:

     ```bash
     bpftrace -e 'kprobe:mlx5e_completion_event { @cq[((struct mlx5_core_cq *)arg0)->cqn] = count(); } interval:s:1 { time("%H:%M:%S "); print(@cq); clear(@cq); }' > "$O/cq-events.txt"
     ```

     Map each CQ number to the RQ or ICOSQ using the devlink dump.
   - **If it doesn't,** capture `trace-cmd record -p function_graph -g mlx5e_napi_poll` for 3 s spanning the recovery instead, and note that the fallback was used.

   Run this on 3 `unpin` cells, reusing task 1's M158 wiring.
3. **Records:** an AN-006 addendum with the three dumps side by side and the first-event CQ of each recovery. Facts only.

## Task 3: the history study (Tuesday September 29 – Friday October 2, desk work alongside runs)

**Question:** how common are receive-path liveness bugs in Linux, what patterns do they follow, and how were they found?

1. **Clone** on a node with disk to spare:

   ```bash
   git clone --filter=blob:none https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git
   ```

2. **Extract candidates:**

   ```bash
   cd linux
   REGEX='stall|hang|lockup|stuck|livelock|starv|lost wake|missed (irq|interrupt|wake)|re-?arm|rx (stop|stall|hang)|napi.*race|race.*napi|busy.?poll|threaded napi|ksoftirqd'
   git log --since=2016-01-01 --until=2026-09-25 --no-merges -i -E --grep="$REGEX" \
     --format='%H%x09%cs%x09%an%x09%s%x09%(trailers:key=Fixes,valueonly,separator=%x3B)' -- \
     net/core/dev.c net/core/dev.h kernel/softirq.c include/linux/netdevice.h \
     drivers/net/ethernet/mellanox/mlx5/core/en_txrx.c \
     drivers/net/ethernet/mellanox/mlx5/core/en_rx.c \
     drivers/net/ethernet/mellanox/mlx5/core/en_main.c \
     drivers/net/ethernet/mellanox/mlx4/en_rx.c \
     drivers/net/ethernet/intel/i40e/i40e_txrx.c \
     drivers/net/ethernet/intel/i40evf/i40e_txrx.c \
     drivers/net/ethernet/intel/iavf/iavf_txrx.c \
     drivers/net/ethernet/intel/ice/ice_txrx.c \
     drivers/net/ethernet/intel/ixgbe/ixgbe_main.c \
     drivers/net/ethernet/broadcom/bnxt/bnxt.c \
     drivers/net/ethernet/google/gve/gve_main.c \
     drivers/net/virtio_net.c > ../study-candidates.tsv
   ```

3. **Triage every candidate.**
   - **Include** only receive-path commits (RX, NAPI, softirq, busy polling, threaded NAPI) whose symptom is a liveness failure: no progress, livelock, starvation, or failure to recover.
   - **Exclude** crashes, leaks, and TX-only bugs.
   - Read each full message with `git show -s --format=%B <hash>`.
4. **Record each included bug** in `analysis/study/rx-liveness-bugs.csv` with these columns:

   | Column | Values |
   | --- | --- |
   | `commit`, `date` | From the log |
   | `subsystem` | core-napi, softirq, busy-poll, threaded, or driver:name |
   | `symptom` | stall, livelock, starvation, lost-event, or no-recovery |
   | `trigger` | overload, affinity change, config toggle, race, reset, or unknown |
   | `sustaining_loop` | Y, N or unknown: does the message describe a state that persists until something external happens? |
   | `recovers_when_load_drops` | Y, N or unknown |
   | `found_by` | Parse `Reported-by:`; mark production, test, fuzzer or unknown |
   | `fix_type` | re-arm, re-check, reschedule, barrier, or other |
   | `introduced_by` | The first hash in the Fixes trailer |
   | `lifetime_days` | Fix commit date minus `git show -s --format=%cs <introduced>` |
   | `confidence` | high or low |

5. **Deliverable** (Friday, October 2): the CSV plus `notes/p2-STUDY-1.md`, reporting:
   - counts by class;
   - median lifetime;
   - the fraction with a sustaining loop;
   - the fraction found in production rather than by tests.

   Target at least 30 included bugs. If there are fewer, report that; don't widen the criteria.

## Task 4: the two mechanism A/Bs (Thursday, October 1)

Each A/B needs its own pre-registered spec first.

**Receive ring size.**
- 8 cells at the default size against 8 at `ethtool -G enp195s0np0 rx 8192`, in the `unpin` arm with the task 1 flood. Check the maximum with `ethtool -g` first.
- **Prediction:** if ring starvation is necessary, the wedge rate falls at least 4× with the larger ring.

**Striding receive queue off.** 8 cells after `ethtool --set-priv-flags enp195s0np0 rx_striding_rq off`. Confirm the flag name with `ethtool --show-priv-flags` first.

**After every ring or flag change:**
- Run the full pre-flight: RSS key, port map, IRQ re-pin.
- **Rediscover the queue-7 NAPI thread;** its ID changes when channels are recreated. Run a 5 s flood at 158k to queue 7 and take the `napi/enp195s0np0-*` thread whose `/proc/<pid>/stat` utime+stime grows most. Parameterize every script that hard-codes `-8263`.

## Task 5: the netdev report (draft Friday, October 2)

Draft it for my review. Include:
- a summary;
- the environment (kernel, firmware from `ethtool -i`, NIC);
- a one-script reproducer;
- the counters and AN-006A timelines;
- task 1's class, which sets the severity;
- task 2's dumps;
- task 4's results;
- the refuted affinity hypothesis;
- the survival data.

If task 1 shows the queue staying dead until manual reset, stop and tell me before drafting: that changes the disclosure route.

## Task 6: `rxfuzz` v0 (October 5–9)

**Monday:** a design note, `specs/p2-RXFUZZ.md`, with these fixed choices.

**Configuration dimensions,** with values per dimension:

| Dimension | Values |
| --- | --- |
| threaded | 0, 1 |
| NAPI thread placement | the IRQ's CPU, its SMT sibling, another core in the same L3 domain, another L3 domain, unpinned |
| adaptive-rx | on, off |
| receive ring | default, 8192; set between cells only |
| striding receive queue | on, off; set between cells only |
| GRO | on, off |
| `busy_read` sysctl | 0, 50 |
| per-device `napi_defer_hard_irqs` | 0, 2 |
| per-device `gro_flush_timeout` (ns) | 0, 200000 |

**Load schedules:**
1. Ramp from 0.3 to 1.5× the reference over 60 s.
2. Flood at 1.5× for 60 s, then drop to 0.6× for 120 s.
3. Five cycles of 20 s at 1.5× and 20 s at 0.3×.
4. A steady 0.9×.

**Perturbations,** once per cell, during load:
- toggle threaded 1→0→1;
- move the NAPI thread's affinity;
- move IRQ 312's affinity;
- change `ethtool -C` moderation.

Never change channels or rings mid-cell.

**Oracles,** evaluated from 1 Hz counters:

| Oracle | Condition |
| --- | --- |
| STALL | The queue's packets flat for ≥6 s while `rx_out_of_buffer` rises |
| METASTABLE | A STALL that persists ≥60 s after the load drops to ≤0.6× the reference |
| COLLAPSE | Delivered below 0.5 × min(offered, reference) for ≥10 s |
| PERMANENT | The probe fails 30 s after all load stops |

**Budget and output:**
- Cells last 5 minutes; about 96 per night, randomly sampled.
- Every hit saves its configuration, schedule, counters and `dmesg`, plus a 10 s trace for the first hit of each class.
- Output goes to `results/p2-RXFUZZ/<date>/<cell>/` and `hits.csv`.

**Minimization:** for each hit, reset one dimension at a time to its default and rerun. Keep only the dimensions whose reset removes the hit. Confirm the minimal configuration 3 times.

**Schedule:** implementation Tuesday to Thursday, the first overnight run Thursday night, results in Friday's memo.

**Second driver:** by October 2, list the CloudLab node types with non-Mellanox NICs (Intel ice or i40e, Broadcom bnxt), from the CloudLab hardware documentation. I'm requesting the r650 this week.

## Due dates

| Due | Deliverable |
| --- | --- |
| Monday, September 28 | Task 1 class; result note |
| Tuesday, September 29 | Task 2 dumps and recovery source |
| Thursday, October 1 | Task 4 A/B results |
| Friday, October 2 | Task 3 study; task 5 netdev draft; Friday memo |
| Monday, October 5 | Task 6 design spec |
| Friday, October 9 | Task 6 first overnight results; memo |
| Friday, October 23 | Gate: a confirmed metastable failure, or at least 2 new bugs, means building toward OSDI '28 or the EuroSys '28 spring deadline; otherwise a HotOS '27 position paper |

## What I need from you, in order

1. Task 1's class, before any interpretation.
2. Any queue that needed a reset, reported immediately.
3. Task 2's dumps side by side.
4. The netdev draft, only after tasks 1, 2 and 4 are in.
