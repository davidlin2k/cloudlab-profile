# costs.env -- the Fig 4 model inputs measured in cal-1/calsmt
# (2026-09-24, plen 64, W1, 450k pps operating point). Consumed by
# p1w5.sh -> p1ctl.py. Re-measure per workload before Fig 6/7 (the
# controller must predict from ITS workload's costs).
C_APP=1060
C_NET=517
SMT=1.24
