# Weekly memo 2026-W39 (Friday review 2026-09-25)

## Figure of the week (per PI, DR-003)
The wedge A/B survival curves (wdiag v2, pre-registered in DR-002).
Time-to-wedge per cell, 8 cells/arm, misaligned vs aligned vs unpinned.

## What is true now (with evidence)
- C-001..C-003 supported-in-draft (note 3 + fig1-3-draft checkpoint).
- C-005 NEW: threaded NAPI wedges under sustained flood on this host --
  24/24 cells at 790k across P2/P3/P4, onset t=2..68, C-state pinning
  refuted as trigger (wedge-ab-verdict checkpoint, AN-003).
- C-004 CONTESTED: the two-cost knee model misses the 64-B knees by
  42-91% on current rows (fig4 adjudication runs today; rule 4 stands
  by if the miss holds on clean cells).
- AN-004 (corrected): launch-delayed sender logs deflate budgets; the
  snd_full column now gates every figure.

## In flight
- wdiag v2 matrix RUNNING on rx (6 arms x 8 reps x 195 s; pre-registered
  predictions in DR-002; recovery test fires at the first confirmed
  wedge per DR-003 decision 4). chain6 (Fig 4 batch) stopped per
  decision 1 -- partial rows kept; the knee re-grade is analysis-only
  on inline placements (decision 9). chain7 (W5) NOT to run (DR-003).
- Counter watch live: ch7_aff_change 22.9M vs 3.9k on ch8 at baseline;
  arm/poll 0.75 (the idiom degrading threaded polls to one interrupt
  per batch -- also the pre-wedge underperformance mechanism).

## Open decisions (PI)
- Disclosure path (decision 4): chosen by the recovery test -- private
  security@kernel.org + mlx5 maintainers if permanent stall (PI reviews
  the draft before it goes out; nothing public until they respond or a
  fix merges), or a netdev patch with Fixes: if it self-recovers.
- r650: APPROVED (decision 7) -- 3 days next week; lspci on day one to
  confirm the Intel NIC (i40e/ice) before building anything.
- Controller name for the abstract's [Name] (unchanged).

## Next week's single goal (per DR-003 run order 5)
The patch and the patched comparison: skip the affinity bailout when
NAPI is threaded + the docs fix, then patched P3/P4 vs P0X and the
default at 0.25x/0.5x/1.5x/2x knee, 5 reps -- which decides the paper
per DR-002's pre-registered outcome table.

## Friday update, 2026-09-25 (due-by-noon pass)

**Figure of the week: the wedge A/B survival curves**
(analysis/out/fig-wedge-ab.{png,pdf}; vision QA PASS). 48 cells, 6
arms. The pre-registered mlx5 poll-affinity hypothesis is REFUTED on
its own rule (AN-006): the aligned arms wedge 6/8 each with aff_change
near zero; the unpinned arm wedges 8/8 fastest (onsets 9-19 s,
C-011 Supported). The wedge itself is real (25/48 + historical 24/24;
C-008 Supported); its mechanism is open pending Monday's ftrace
(DR-004 task 3). Recovery: SELF-RECOVERING (probe 10k pps full
delivery, wp50 11 us) -> performance bug, netdev patch route, no
embargo.

**DR-004 claims movements:** C-007/C-008/C-011 promoted Supported;
C-012 (scheduler-wait mechanism, 34-60 us vs 4 us) and C-013
(interrupt-core ceiling) new Supported; C-014 now Supported (the
metric fix below); C-007's numbers revised (Pending re-promotion: with
corrected costs the raw model passes, 2.8%/6.2% miss, no fitted
constant); C-009 Suspended.

**The utilization metric was fixed (DR-004 task 1).** /proc/stat
charges sub-tick IRQ-context bursts to idle on this kernel
(CONFIG_IRQ_TIME_ACCOUNTING=n): P0X 390k measured 0.22 s of busy
against a 36.53 s PMU truth (166x). cpuN_busy_s now comes from
ref-cycles/TSC bracketed to the consumer's window (AN-007, FINDINGS
p1-LADDER.3). Figure 3 rebuilt: the app thread accounts for only
43-55% of the real per-packet CPU in every placement (2026-2935 ns
total; QA PASS). Knee model with corrected costs: P0 64 B 2.8% miss,
P0X 6.2% miss (FINDINGS p1-LADDER.4).

**Control-plane stall: falsified (DR-004 task 2, today).** 45/45 runs
at 2x the knee complete at idle speed (netlink, netns, drop_caches all
<=2x idle medians; no timeout). No claim; the pre-registration's
">1 s" prediction did not hold (notes/p1-LADDER-2.md).

**Decision 8 closed:** the 108 us co-location latency = scheduler wake
wait behind receive processing (C-012); NOT interrupt moderation
(adaptive-rx off changes nothing). Memcached check pending (task 4).

### For the PI
- r650 request (DR-004 decision 7): listed under your actions; the
  web portal login and the CLI/cert path are both unavailable to me.
- C-007's numbers changed with the metric fix; re-promotion of the
  revised wording is yours.
- The netdev report stays drafted-if-needed for Wednesday's review
  (task 3 gate, October 5).

### Next week
Mon: wedge tracing (task 3, one day strict). Tue-Fri: memcached W3
(task 4, pre-register first). Mon+: workshop paper (task 5, outline
due Sep 29).

### Friday update (2026-09-25, before noon) -- the week closed early

The plan above ("next week") ran TODAY instead.

**Task 3 closed (deliverable).** The wedge traced 3/3 under trace-cmd:
the onset timeline (queue 7's kthread completes a normal poll, sleeps,
and no interrupt or completion arrives for 18.7 s while the wire and
drops advance) and the recovery timeline (a fresh IRQ-312 interrupt
19 ms-8.9 s after flood stop, no timer involved). Facts only in
AN-006A; interpretation held for your read ("we don't guess").

**Task 4 closed (W3).** Spec pre-registered first (knee from low-load
costs, kill criterion, >=5x wake-delay), then 69 cells (memcached -t 1,
ntuple to queue 7, mutilate open-loop, five senders). Verdicts on the
frozen rules: knee prediction FAILS (3.1x off -- TCP cost falls with
load); the kill criterion KILLS the transport-independence claim (at
2x knee, delivered holds 169%/194% of knee goodput -- flow control
plateaus, the collapse does not cross to TCP); >=5x wake-delay FAILS
on request p99 (1.45x) though the scheduler mechanism replicates at
12.5x (mc-worker wake wait 50 us vs 4 us -- C-012's memcached clause
is now Supported on UDP and TCP); the SLO prediction FAILS as stated.
What stands and carries Fig. 7: separation beats co-location on TCP
at every load above the lightest (SLO frac 0.999 vs 0.829 and p99 645
vs 1475 us at 1.5x knee; +15% goodput at 2x). C-005 and C-015
Supported; C-016/017/018 Refuted; claims appended, nothing bent
(notes/p1-W3.md).

**Task 5 started.** Workshop outline drafted two days early
(workshop-outline.md; every numbered sentence mapped to a finding ID,
Supported claims only). Fig. 7's plot script exists and passed vision
QA (analysis/p1_fig_w3.py).

### For the PI -- updated items
- NEW decision item (Sept 29 review): include the pre-registered
  negative results (the kill-criterion story) in the workshop paper or
  cut to positive results only? The rules say "only Supported claims";
  the refutations are recorded as outcomes, not claims.
- C-007 re-promotion still yours (Section 4 of the workshop paper
  needs it).
- C-005 is now Supported (W3 resolved its condition).
- Next runs (no date gate): W4 (memcached + flood, Fig. 8), then the
  Fig. 5-10 skeleton order; Fig. 7's remaining policies wait on the
  P1 kernel session, P5 and P7.
